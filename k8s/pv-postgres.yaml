apiVersion: v1
kind: PersistentVolume
metadata:
 name: dbpv
 namespace: postgres
spec:
 capacity:
   storage: 10Mi
 accessModes:
   - ReadWriteOnce
 persistentVolumeReclaimPolicy: Retain
 storageClassName: manual
 hostPath:
   path: "/mnt/data"
